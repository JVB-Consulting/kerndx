<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FailureAction__c</fullName>
    <description>What to do if applying this rule throws an error at runtime (for example, a bad regex or an unparseable JSON payload). Log and Continue keeps the original value and records the failure. Write Failure Marker replaces the entire field value with a sentinel marker so the original is never persisted. Block DML refuses the save and surfaces an error to the caller.</description>
    <fieldManageability>SubscriberControlled</fieldManageability>
    <inlineHelpText>Log and Continue keeps the original on error. Write Failure Marker replaces the field with a sentinel. Block DML refuses the save. Pick the strictest option your compliance requires.</inlineHelpText>
    <label>Failure Action</label>
    <required>true</required>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetDefinition>
            <sorted>false</sorted>
            <value>
                <fullName>LogAndContinue</fullName>
                <default>true</default>
                <label>Log and Continue</label>
            </value>
            <value>
                <fullName>WriteFailureMarker</fullName>
                <default>false</default>
                <label>Write Failure Marker</label>
            </value>
            <value>
                <fullName>BlockDml</fullName>
                <default>false</default>
                <label>Block DML</label>
            </value>
        </valueSetDefinition>
    </valueSet>
</CustomField>
