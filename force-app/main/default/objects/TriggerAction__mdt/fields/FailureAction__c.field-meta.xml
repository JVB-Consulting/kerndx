<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FailureAction__c</fullName>
    <description>Controls how this trigger action handles unhandled errors raised during dispatch — flow runtime faults for flow actions, uncaught Apex exceptions for Apex actions. Log and Continue (default) records the error in the audit log and lets the save proceed — recommended for non-critical actions like notifications, derived field updates, or supplemental logging. Block DML stops the save and surfaces the action&apos;s error message back to the user — recommended for validation-style actions where the trigger should reject records that fail the action&apos;s checks. Governor exceptions (CPU limit, SOQL limit, etc.) always propagate regardless of this setting. Defaults to Log and Continue.</description>
    <fieldManageability>SubscriberControlled</fieldManageability>
    <inlineHelpText>Log and Continue (default) records the error and lets the save proceed. Block DML stops the save and shows the action&apos;s error to the user. Pick Block DML for validation-style actions; Log and Continue for everything else. Applies to flow runtime faults and Apex action exceptions.</inlineHelpText>
    <label>Failure Action</label>
    <required>false</required>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetDefinition>
            <sorted>false</sorted>
            <value>
                <fullName>LogAndContinue</fullName>
                <default>true</default>
                <label>Log and Continue</label>
            </value>
            <value>
                <fullName>BlockDml</fullName>
                <default>false</default>
                <label>Block DML</label>
            </value>
        </valueSetDefinition>
    </valueSet>
</CustomField>
