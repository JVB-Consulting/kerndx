<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>IdempotencyKey__c</fullName>
    <description>Preserves the idempotency key from the failed API call. When retried, this key is copied to the new API call so the external system treats the retry as a repeat of the original request.</description>
    <externalId>false</externalId>
    <inlineHelpText>Idempotency key from the original failed API call, preserved for retry.</inlineHelpText>
    <label>Idempotency Key</label>
    <length>255</length>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
