<?xml version="1.0" encoding="UTF-8"?>
<CustomObject xmlns="http://soap.sforce.com/2006/04/metadata">
    <description>Registers a single post-trigger action: one Apex class that runs once at the end of a trigger transaction, after all trigger actions on every object have completed. Use for cross-object work that cannot be done from inside an individual trigger action — aggregate audit emission, asynchronous job enqueue, transaction-scoped telemetry. Post-trigger actions must not perform DML; the dispatcher rethrows if they do.</description>
    <label>Post-Trigger Action</label>
    <pluralLabel>Post-Trigger Actions</pluralLabel>
    <visibility>Public</visibility>
</CustomObject>
