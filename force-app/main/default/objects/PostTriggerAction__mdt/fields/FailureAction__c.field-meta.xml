<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>FailureAction__c</fullName>
    <description>Controls how this post-trigger action handles unhandled Apex exceptions. Log and Continue (default) records the error in the audit log and lets the transaction proceed — recommended for non-critical post-actions like notifications, async enqueues, or supplemental telemetry. Block DML stops the save and surfaces the action&apos;s error message back to the user — recommended only when the post-action must be guaranteed to complete before any data is committed. Governor exceptions (CPU limit, SOQL limit, etc.) always propagate regardless of this setting. A post-trigger action that performs DML always fails the transaction, regardless of this setting, because DML is contractually prohibited inside post-trigger actions.</description>
    <fieldManageability>SubscriberControlled</fieldManageability>
    <inlineHelpText>Log and Continue (default) records the error and lets the save proceed. Block DML stops the save and shows the action&apos;s error to the user. DML performed by the post-action always fails the transaction regardless of this setting.</inlineHelpText>
    <label>Failure Action</label>
    <required>false</required>
    <type>Picklist</type>
    <valueSet>
        <restricted>true</restricted>
        <valueSetDefinition>
            <sorted>false</sorted>
            <value>
                <fullName>LogAndContinue</fullName>
                <default>true</default>
                <label>Log and Continue</label>
            </value>
            <value>
                <fullName>BlockDml</fullName>
                <default>false</default>
                <label>Block DML</label>
            </value>
        </valueSetDefinition>
    </valueSet>
</CustomField>
